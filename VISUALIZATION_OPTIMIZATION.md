# 可视化优化方案 — 从文字为主到图表为主

> 日期: 2026-08-21
> 目的: 当前界面信息虽已清晰，但可视化程度不足。需要从"文字+CSS条形"升级为"真图表+树形图"，让用户一眼看懂执行全貌。
> 命名: 保持原有命名不变

---

## 一、问题诊断

当前 9 个标签页的可视化手段仅限于**纯 CSS 条形图**（`.bar-fill`、`.speed-bar`、`.tb-fill`、堆叠色块），没有使用任何图表库。具体表现：

| 标签页 | 当前展示方式 | 问题 |
|--------|-------------|------|
| 概览 | 8 个文字统计卡片 + CSS 条形图 | 数字罗列为主，缺乏一眼可读的视觉焦点 |
| 时间线 | Turn → Step → Event 三级文字折叠列表 | 纯文字缩进，看不出树形拓扑结构 |
| 工具流程 | 文字列表 + 可展开 JSON 面板 | 工具间的调用顺序和依赖关系不可见 |
| 推理过程 | 合并文本 + CSS 速度条 | 速度条仅是一行横条，无法体现推理节奏变化 |
| 任务清单 | 快照列表 + 状态文字标签 | 任务状态流转不可视 |
| 审批流程 | 文字配对列表 | 审批的时间线感和等待时长不可见 |
| Token 统计 | CSS 总量条 + 逐条堆叠条 | 堆叠条太扁平，无法体现 Token 随时间的变化趋势 |
| 事件搜索 | 文字列表 + 分页 | 可保留，搜索结果本身需要文字 |
| 原始数据 | JSONL 只读视图 | 可保留，原始数据适合代码风格 |

**核心问题**: 文字占比约 80%，图表占比约 20%（且仅是 CSS 条形）。用户需要大量阅读才能理解执行全貌，与"可视化"的预期不符。

**目标**: 图表占比提升到 70%，文字降至 30%（仅做标注和详情）。

---

## 二、箭头指示器

### 2.1 问题

当前侧边栏和主内容区之间没有视觉连接。用户首次打开时，看不出"左边是 Session Log 原始日志，右边是可视化结果"的流向关系。

### 2.2 设计

在侧边栏右边缘添加一条品牌色箭头，从 Session Log 列表指向可视化看板：

```
┌──────────────┐         ┌──────────────────────┐
│ Session Log  │         │  可视化看板           │
│              │  ════>  │                      │
│ • session-1  │  可视化  │  📊 环形图  📈 甘特   │
│ • session-2  │         │  🌳 树形图  🔗 流程   │
│ • session-3  │         │                      │
└──────────────┘         └──────────────────────┘
```

**具体规格**:

- 位置: 侧边栏与主内容区交界处，垂直居中
- 样式: 品牌色箭头 + "可视化"文字标签
- 尺寸: 箭头宽度约 36px，文字标签 12px
- 交互: 点击箭头可折叠/展开侧边栏
- 颜色: 使用当前品牌色（与现有配色方案一致）

### 2.3 涉及文件

- `static/index.html` — 在 `#sidebar` 和 `#main` 之间添加箭头元素
- `static/style.css` — 添加 `.arrow-bridge` 样式
- `static/app.js` — 添加点击折叠/展开交互

---

## 三、可视化改造（逐标签页）

### 3.1 概览页 → 可视化仪表盘

**当前**: 8 个文字统计卡片 + 事件类型分布 CSS 条形图 + 分组统计 CSS 条形图 + 元信息表格

**改为**:

| 区域 | 当前 | 改为 | 图表类型 |
|------|------|------|---------|
| 顶部 KPI | 8 个文字数字卡片 | 精简为 4 个 KPI 大数字卡片（总事件/Turn数/工具调用/耗时） | KPI 卡片 |
| 事件分布 | CSS 条形图 | **环形图** — 一眼看出 reasoning 占 34%、tool-call 占 28% | ECharts Donut |
| 分组统计 | CSS 条形图 | **横向条形图** — 按组排列，可点击筛选 | ECharts Bar |
| 元信息 | 文字表格 | 保留为底部可折叠面板 | 文字（折叠态） |

**布局示意**:

```
┌─────────────────────────────────────────────┐
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 9285 │ │  2   │ │ 224  │ │15m23s│      │
│  │总事件 │ │Turns │ │工具  │ │耗时  │      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│                                             │
│  ┌─────────────┐  ┌──────────────────┐    │
│  │   环形图     │  │  横向条形图       │    │
│  │  事件分布    │  │  分组统计         │    │
│  │  (Donut)    │  │  (点击可筛选)     │    │
│  └─────────────┘  └──────────────────┘    │
│                                             │
│  [▼ 会话元信息]                              │
└─────────────────────────────────────────────┘
```

---

### 3.2 时间线 → SVG 树形图

**当前**: Turn → Step → Event 三级文字折叠列表，事件行显示类型标签 + 摘要 + 时间

**改为**: SVG 树形图，从左到右展开

**设计规则**:

- 第一层: **Turn 节点** — 圆形节点，半径较大，显示"Turn 1"+事件总数+耗时
- 第二层: **Step 节点** — 圆形节点，显示"Step 1"+事件数+主要工具图标
- 第三层: **合并事件组节点** — 小圆形，同类事件合并（如 reasoning 4899 条合并为一个节点）
- 连接线: 曲线（贝塞尔），默认灰色，当前展开路径高亮为品牌色
- 节点颜色: 按 14 组配色方案着色
- 交互: 点击节点展开/折叠；悬停显示摘要 tooltip；点击节点高亮整条路径

**布局示意**:

```
●─── Turn 1 (5234, 8m30s)
├─── ●─── Step 1 (12, 1.2s, 初始化)
│    ├── ○ session
│    ├── ○ preset
│    └── ○ user/message
├─── ●─── Step 2 (8, 0.8s, 读取)
│    ├── ○ tool/call:read
│    └── ○ tool/result
└─── ●─── Step 3 (5012, 6m40s, 分析)
     ├── ○ reasoning (4899)
     ├── ○ assistant/output (113)
     └── ○ tool/call:write
```

**技术**: 纯 SVG 手写，无外部依赖。使用 `<path>` 绘制曲线连接，`<circle>` 绘制节点，`<text>` 显示标签。折叠/展开通过控制子节点 `display` 实现。

---

### 3.3 工具流程 → SVG 网络流程图

**当前**: 工具调用文字列表，可展开显示参数/错误/结果/时序（JSON 格式）

**改为**: SVG 网络流程图

**设计规则**:

- 每个工具调用一个节点，按时间从左到右排列
- 节点形状: 圆角矩形
- 节点大小: 按耗时比例（耗时越长，节点越大）
- 节点颜色: 按工具类型着色（read=蓝、write=橙、grep=青、pwsh=紫等）
- 连接箭头: 从前一个工具指向后一个工具，表示调用顺序
- 节点标签: 工具名 + 参数摘要（如 `read("REQUIREMENTS.md")`）
- 交互: 点击节点弹出详情模态框（保留当前模态框逻辑）
- 同步工具: 并行调用用分叉箭头表示

**布局示意**:

```
┌─ read ──┐      ┌─ glob ──┐      ┌─ write ─┐
│REQ.md   │ ──>  │ **/*.ts │ ──>  │REQ.md   │
│293行    │      │ 12 files│      │15KB     │
└─────────┘      └─────────┘      └─────────┘
      │
      ├────────> ┌─ grep ───┐
      │          │session   │
      │          │8 matches │
      │          └──────────┘
      │
      └────────> ┌─ pwsh ───┐
                 │npm build │
                 │success   │
                 └──────────┘
```

**技术**: 纯 SVG 手写。节点用 `<rect rx="8">`，箭头用 `<marker>` + `<path>`。

---

### 3.4 推理过程 → 面积图

**当前**: 合并推理文本 + CSS 速度条（每分片耗时）+ 关键词高亮

**改为**: 面积图 + 可折叠文本面板

**设计规则**:

- 上半部分: **面积图** — X 轴为时间（分片序号），Y 轴为每分片字符数，面积填充用品牌色
- 面积图颜色深浅: 按分片速度（字符/秒）渐变，快速推理=深色，慢速推理=浅色
- 下半部分: 合并后的推理文本，默认折叠，点击展开
- 关键词高亮: 保留现有逻辑
- 交互: 鼠标移到面积图某点，显示该分片的文本预览 tooltip

**技术**: ECharts 面积图（area chart），`xAxis` 为分片序号，`yAxis` 为字符数，`series.areaStyle` 控制填充。

---

### 3.5 任务清单 → SVG 状态流程图

**当前**: 快照列表 + 状态文字标签（pending/in_progress/completed）

**改为**: SVG 状态流程图

**设计规则**:

- 每个任务一个节点，按状态分三列排列:
  - 左列: pending（待处理）— 灰色
  - 中列: in_progress（进行中）— 橙色/警告色
  - 右列: completed（已完成）— 绿色/成功色
- 连接箭头: 从 pending 指向 in_progress，从 in_progress 指向 completed
- 节点标签: 任务内容前 20 字
- 快照对比: 不同时间点的任务状态变化用虚线连接
- 交互: 点击节点展开任务详情

**布局示意**:

```
  Pending          In Progress       Completed
  ────────        ──────────        ──────────
                                    ● 解析数据结构
                                    ● 编写解析器
  ● 测试覆盖        ● 编写可视化
                                    ● 配色方案
```

**技术**: 纯 SVG 手写，三列布局，节点用圆形，连线用箭头。

---

### 3.6 审批流程 → SVG 垂直时间线

**当前**: 审批配对列表，显示工具名 + 结果 + 原因 + 等待时长

**改为**: SVG 垂直时间线

**设计规则**:

- 垂直主线: 从上到下表示时间流逝
- 每个审批一个节点:
  - ⚠️ 图标表示审批请求
  - ✅ 绿色表示通过
  - ❌ 红色表示拒绝
- 节点间距: 按等待时长比例排列（等待越久，间距越大）
- 节点旁标注: 工具名 + 等待时长（如"write — 等待 5.6s"）
- 点击节点: 展开审批原因和完整详情

**布局示意**:

```
│
●  ⚠️ write → ✅ 通过 (5.6s)
│
│  (等待 5.6s)
│
●  ⚠️ pwsh → ✅ 通过 (3.2s)
│
│  (等待 3.2s)
│
●  ⚠️ edit → ❌ 拒绝 (12.1s)
│
```

**技术**: 纯 SVG 手写，垂直 `<line>` + `<circle>` 节点 + `<text>` 标注。

---

### 3.7 Token 统计 → 双图表

**当前**: 总量 CSS 条形图 + 逐条消息堆叠 CSS 条

**改为**: 环形图 + 堆叠面积图

**设计规则**:

- 上半部分: **环形图** — 输入/输出/推理/缓存读取四段比例
- 下半部分: **堆叠面积图** — X 轴为消息序号，Y 轴为 Token 数，四层堆叠（输入/输出/推理/缓存）
- 颜色: 输入=品牌色，输出=浅品牌色，推理=蓝色系，缓存=灰色
- 交互: 点击环形图某段，面积图高亮对应层

**技术**: ECharts 环形图 + 堆叠面积图。

---

### 3.8 事件搜索 → 保留（微调）

**当前**: 全文搜索 + 类型/分组下拉 + 分页

**保留**: 搜索结果本身需要文字，列表形式合理。

**微调**:
- 每条搜索结果前增加类型色块图标（小圆点，按 14 组配色着色）
- 结果项布局从纯文字改为: 色块 + 类型标签 + 摘要 + 时间

---

### 3.9 原始数据 → 保留

**当前**: JSONL 只读视图，行号 + 类型标签 + 原始行 + 高亮当前行

**保留**: 原始数据适合代码风格展示，无需图表化。

---

## 四、技术方案

### 4.1 图表库选择

| 技术 | 用途 | 引入方式 | 理由 |
|------|------|---------|------|
| **ECharts** | 环形图、堆叠面积图、横向条形图 | CDN: `https://cdn.jsdelivr.net/npm/chart.js@4` 或 ECharts CDN | 配置驱动，开箱即用，适合标准图表类型 |
| **纯 SVG** | 树形图、网络流程图、状态流程图、垂直时间线 | 手写，无依赖 | 需要自定义节点/连线/交互，SVG 更灵活可控 |
| **CSS Grid/Flex** | 仪表盘布局、KPI 卡片 | 已有基础 | 无需额外引入 |

**选择 ECharts 而非 D3.js 的理由**:
- ECharts 配置驱动，几行配置即可生成环形图/面积图/条形图
- D3.js 学习曲线陡峭，需要手动操作 DOM，开发成本高
- 树形图和流程图用纯 SVG 更灵活，ECharts 的图类型不够定制化

### 4.2 渐进增强策略

- 页面加载时先渲染 SVG 内容（无依赖，立即可见）
- ECharts 异步加载，加载完成后替换对应容器
- ECharts 加载失败时，容器内保留 CSS 条形图作为 fallback

### 4.3 性能注意

- 面积图数据量大时（9000+ 条消息），使用 ECharts 的 `dataZoom` 缩放和 `large` 模式
- SVG 树形图折叠的节点不渲染子节点 DOM，避免 9000+ 个 DOM 节点
- 事件搜索分页保持 200 条/页

---

## 五、实施步骤

| 阶段 | 内容 | 涉及文件 | 优先级 | 工作量 |
|------|------|---------|--------|--------|
| 1 | 箭头指示器 | `index.html` `style.css` `app.js` | P0 | 小 |
| 2 | 概览页: 环形图 + 横向条形图 | `app.js` `style.css` + 引入 ECharts | P0 | 中 |
| 3 | 时间线: SVG 树形图 | `app.js` `style.css` | P0 | 大 |
| 4 | Token 统计: 环形图 + 堆叠面积图 | `app.js` + ECharts | P1 | 中 |
| 5 | 工具流程: SVG 网络流程图 | `app.js` `style.css` | P1 | 大 |
| 6 | 审批流程: SVG 垂直时间线 | `app.js` `style.css` | P1 | 中 |
| 7 | 推理过程: 面积图 | `app.js` + ECharts | P1 | 中 |
| 8 | 任务清单: SVG 状态流程图 | `app.js` `style.css` | P2 | 中 |
| 9 | 事件搜索: 增加色块图标 | `app.js` `style.css` | P2 | 小 |

**建议实施顺序**: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9

阶段 1-3 是 P0，解决最核心的"不够可视化"问题。阶段 4-7 是 P1，完成剩余图表化。阶段 8-9 是 P2，收尾微调。

---

## 六、前后对比总结

| 维度 | 当前 | 优化后 |
|------|------|--------|
| 侧边栏→主区关系 | 无视觉连接 | 品牌色箭头 + "可视化"标签 |
| 概览页 | 8 个文字卡片 + CSS 条形图 | 4 个 KPI 卡片 + 环形图 + 横条图 |
| 时间线 | 三级文字折叠列表 | SVG 树形图，节点+曲线连接 |
| 工具流程 | 文字列表 + JSON 展开 | SVG 网络流程图 |
| 推理过程 | 合并文本 + CSS 速度条 | 面积图 + 可折叠文本面板 |
| 任务清单 | 快照列表 + 状态标签 | SVG 状态流程图（三列） |
| 审批流程 | 文字配对列表 | SVG 垂直时间线 |
| Token 统计 | CSS 堆叠条 | 环形图 + 堆叠面积图 |
| 事件搜索 | 纯文字列表 | 文字列表 + 类型色块图标 |
| 原始数据 | JSONL 代码视图 | 保留不变 |
| 图表库 | 无 | ECharts + 纯 SVG |
| 文字占比 | ~80% | ~30%（仅做标注和详情） |
| 图表占比 | ~20%（仅 CSS 条形） | ~70%（真图表 + SVG 图） |

---

## 七、注意事项

1. **保持 14 组配色方案不变**: 现有的 14 组颜色方案在所有图表中继续使用，确保视觉一致性。
2. **保留搜索功能**: 全文搜索框保留，搜索时自动展开匹配的 Turn/Step 节点。
3. **保留模态框**: 点击任意图表节点的事件详情模态框逻辑保留。
4. **性能**: 大数据量会话（9000+ 事件）渲染时，折叠的节点不渲染子项 DOM，使用懒加载。
5. **渐进增强**: ECharts 异步加载，加载前显示 CSS fallback，加载失败不影响基本功能。
6. **响应式**: 图表容器使用百分比宽度，适配不同屏幕尺寸。
