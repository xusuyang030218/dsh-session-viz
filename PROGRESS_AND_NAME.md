# DSH Session 可视化 — 进度条与命名优化方案

---

## 一、工具命名优化

### 1.1 当前问题

当前名称：**"会话日志查看器"**

问题：
- 太泛：像系统自带工具，没有辨识度
- 太技术：非技术用户不知道"会话日志"是什么
- 太被动："查看器"暗示只能看，不能交互分析

### 1.2 命名方案

| 候选名 | 英文代号 | 含义 | 适合人群 |
|--------|----------|------|----------|
| **AgentTrace** | agent-trace | 智能体轨迹 — 追踪 AI 做了什么 | 技术用户 |
| **SessionLens** | session-lens | 会话透镜 — 透视 AI 执行过程 | 通用 |
| **DeepTrace** | deep-trace | 深度追踪 — 深入分析执行细节 | 技术用户 |
| **FlowScope** | flow-scope | 流程显微镜 — 放大 AI 决策流程 | 通用 |
| **AgentReplay** | agent-replay | 智能体回放 — 回放 AI 执行过程 | 通用 |

### 1.3 推荐

**推荐名称: AgentTrace（智能体轨迹）**

理由：
- "Trace"（轨迹）直击核心价值 — 追踪 AI 的执行路径
- "Agent"明确关联智能体，不限于 DSH，通用性强
- 简短好记，英文 11 字符，中文 5 字
- 简称 `at` 或 `trace` 作为命令行工具名也很自然

**界面标题栏展示**:

```
┌─────────────────────────────────────────────────────┐
│ ◈ AgentTrace          解析sessionlog并可视化        │
│   智能体轨迹          D:\dsh-session-viz             │
└─────────────────────────────────────────────────────┘
```

- 左侧：图标 + 工具名（粗体）+ 副标题（灰色小字）
- 中间：会话标题（截断时鼠标悬停显示全文 tooltip）
- 右侧：路径 + 事件数 + 操作按钮

---

## 二、进度条优化

### 2.1 当前问题

当前完全没有进度条。只有标题栏的"9285 条"静态数字，用户无法感知：
- 已浏览到整体数据的什么位置
- 当前 Turn/Step 在整个会话中处于哪里
- 加载/解析大数据量的进度

### 2.2 进度条设计 — 四层进度体系

#### 进度条 1: 全局滚动进度条（窗口底部）

**位置**: 窗口最底部，贯穿全宽

**形态**: 极细横条（3px 高），不打扰主内容

```
左侧列表                    右侧详情
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ...内容...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
█░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 18%     ← 3px 细条
```

**内容**:
- 彩色分段：每个色段代表一个 Turn，宽度按事件数比例
  - Turn 1（蓝色段）占 56%
  - Turn 2（绿色段）占 44%
- 指示器：白色竖线标记当前浏览位置，显示百分比
- 鼠标悬停：显示 "Turn 1, Step 3, 第 1680/9285 条"

**交互**:
- 点击进度条 → 跳转到对应位置
- 拖拽指示器 → 快速定位

#### 进度条 2: Turn/Step 结构进度条（左侧列表顶部）

**位置**: 左侧列表区域顶部，搜索框下方

**形态**: 紧凑的水平进度链

```
┌──────────────────────────────────┐
│ Turn 1 ████████████░░░░ Turn 2  │
│ Step 3/197  ▸推理中 ▸工具调用     │
└──────────────────────────────────┘
```

**设计**:
- 第一行：Turn 级进度，每个 Turn 一个色块，当前 Turn 高亮
- 第二行：当前 Turn 内的 Step 进度，显示 "Step 3/197" + 当前活动类型
- 色块宽度按事件数比例分配
- 当前展开的 Step 用闪烁边框标记

**交互**:
- 点击 Turn 色块 → 跳转到该 Turn
- 点击 Step → 展开该 Step 的事件

#### 进度条 3: 单步执行进度（详情面板内）

**位置**: 右侧详情面板，选中某个 Step 时

**形态**: 圆环进度 + 横条细分

```
┌─ Step 3 执行进度 ──────────────┐
│                                │
│     ╭───╮                      │
│    │ 62% │   推理 ████░░ 4899条 │
│     ╰───╯    输出 ██░░░░ 113条  │
│              工具 █░░░░░ 8次    │
│              审批 ░░░░░ 0次    │
│                                │
│  总计 5012条 | 耗时 6分40秒     │
│                                │
└────────────────────────────────┘
```

**设计**:
- 左侧圆环：当前 Step 在整个会话中的位置百分比
- 右侧横条：Step 内各事件类型占比，按颜色方案着色
- 底部：总条数 + 耗时

#### 进度条 4: 加载进度（首次打开会话时）

**位置**: 全屏遮罩居中

**形态**: 环形进度 + 文字

```
        ╭───────╮
       │  解析中  │
       │   45%   │
        ╰───────╯

    正在解析 9285 条事件...
    [██████████░░░░░░░░░░] 45%
    
    已识别: 22 种事件类型
    已配对: 224 组工具调用
    已合并: 3199 条推理分片
```

**设计**:
- 环形进度 + 线性进度双重展示
- 实时显示解析状态（识别类型、配对工具、合并分片）
- 完成后淡出，进入摘要卡片

---

## 三、总结

| 优化项 | 当前 | 优化后 |
|--------|------|--------|
| 工具名 | 会话日志查看器 | **AgentTrace**（智能体轨迹） |
| 全局进度 | 无 | 底部 3px 彩色分段进度条，可点击跳转 |
| Turn/Step 进度 | 无 | 列表顶部紧凑进度链，显示当前位置 |
| 单步进度 | 无 | 详情面板圆环+横条，展示事件类型占比 |
| 加载进度 | 无 | 首次打开全屏环形进度+实时解析状态 |
