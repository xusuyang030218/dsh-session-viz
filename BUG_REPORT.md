# AgentTrace 问题诊断报告

> 基于 4 张运行截图分析
> 日期: 2026-08-21

---

## 问题总览

| 编号 | 问题 | 严重度 | 出现位置 |
|------|------|--------|----------|
| P1 | 加载卡在 30%，事件数为 0 | 严重 | 截图 1 |
| P2 | 摘要页只显示文件修改列表，缺少核心摘要内容 | 严重 | 截图 2 |
| P3 | 多个节点显示"(无预览)"，内容未渲染 | 严重 | 截图 3、4 |
| P4 | 工具调用流节点显示红色箭头，渲染异常 | 中等 | 截图 4 |
| P5 | 推理过程文本被截断 | 中等 | 截图 4 |
| P6 | 事件数量不一致（0 → 19962） | 中等 | 截图 1 vs 2 |
| P7 | 右侧统计数字与左侧树节点不匹配 | 低 | 截图 3、4 |

---

## 逐项分析

### P1: 加载卡在 30%，事件数为 0

**现象**: 截图 1 显示环形进度停在 30%，文字"正在解析事件..."和"加载中..."，但标题栏显示"0 条事件"。

**可能原因**:

1. **JSONL 解析失败**: 解压后的 session.json 文件格式不符合预期，解析器在 30% 处遇到格式错误中断
2. **文件路径扫描错误**: `D:\dsh-session-viz\decoded-sessions\` 目录结构与分析器预期不一致，扫描到 30% 的文件后无法继续
3. **内存不足**: 单个会话 19962 条事件，解析时一次性加载到内存导致 OOM
4. **zstd 解压异常**: 部分会话文件未正确解压，或解压后编码不是 UTF-8
5. **异步 Promise 未正确 resolve**: 前端 fetch 请求发出但后端响应在 30% 后中断

**排查方向**:
- 检查后端 API `/api/sessions/{id}/events` 是否返回了完整数据
- 检查浏览器控制台 (F12) 是否有 JavaScript 错误
- 检查后端日志是否有解析异常堆栈

---

### P2: 摘要页只显示文件修改列表

**现象**: 截图 2 显示"摘要"标签页被选中，但内容只有 16 条文件修改记录（lib/models.py、lib/parser.py 等），缺少需求文档中定义的摘要内容。

**缺失内容**（按需求文档应有）:
- 用户需求原文
- AI 执行概要（几轮对话、几个步骤）
- 工具调用统计卡片（读取/写入/搜索次数）
- 审批统计
- Token 用量统计
- 会话元信息（模型、工作目录、耗时）

**可能原因**:

1. **摘要数据未从后端获取**: 后端 API `/api/sessions/{id}` 未返回摘要数据，前端 fallback 到文件修改列表
2. **摘要生成逻辑未实现**: 后端只实现了事件列表 API，未实现摘要聚合逻辑
3. **前端路由错误**: 摘要标签页错误地渲染了文件变更组件而非摘要组件
4. **数据模型不匹配**: 后端返回的摘要数据结构与前端期望的不一致，渲染失败后 fallback 到简单列表

---

### P3: 多个节点显示"(无预览)"

**现象**: 截图 3 和截图 4 中，多个事件节点显示"(无预览)"：
- 助手输出 → "(无预览)"
- 推理过程 → "(无预览)"（截图 3）
- 工具调用流 → "(无预览)"（截图 4）
- 助手输出 → "(无预览)"

**可能原因**:

1. **chunks 合并逻辑未实现**: reasoning-chunks 和 assistant/chunk 的分片合并功能未完成，前端拿到的是原始分片数组，不知道如何拼接为预览文本
2. **预览生成函数缺失**: 事件类型到预览文本的映射函数（需求文档 2.4 节定义的）未实现，所有类型返回"(无预览)"作为 fallback
3. **data 字段为空**: 解析器未正确提取 `data.texts[]`、`data.chunk`、`data.args[]` 等字段，导致预览生成时拿不到数据
4. **事件类型判断错误**: 解析器将事件 type 识别为其他值，未匹配到预览生成逻辑

**影响**: 这是核心功能缺失。用户看到 5 个节点中 3 个显示"(无预览)"，工具失去使用价值。

---

### P4: 工具调用流节点显示红色箭头

**现象**: 截图 4 中"工具调用流"节点旁边有红色向下箭头，其他节点是黄色箭头。

**可能原因**:

1. **渲染异常**: 工具调用流节点的展开/折叠逻辑有 bug，红色箭头是 CSS 样式错误的产物
2. **数据解析错误**: tool-call-chunks 的 `data.args[]` 字段解析失败，节点标记为异常状态
3. **展开后内容为空**: 点击展开后没有子内容（因为 chunks 未合并），UI 以红色标记异常
4. **callId 配对失败**: tool/call 和 tool/result 的 callId 配对逻辑有误，无法正确配对导致节点状态异常

---

### P5: 推理过程文本被截断

**现象**: 截图 4 中推理过程显示"user wants me to optimize/modify base"，文本明显被截断。

**可能原因**:

1. **预览长度限制过短**: 预览文本截取了前 N 个字符，N 设置得太小
2. **分片合并不完整**: 只取了第一个 reasoning-chunks 分片的第一个 text 元素，未合并所有分片
3. **英文内容未做截断省略号处理**: 截断位置在单词中间，没有加"..."提示还有后续内容

---

### P6: 事件数量不一致

**现象**: 截图 1 显示"0 条事件"，截图 2 显示"19,962 条事件"。

**可能原因**:

1. **加载状态 vs 加载完成**: 截图 1 是加载中状态（30%），事件尚未解析完成所以显示 0；截图 2 是加载完成后状态，显示 19962
2. **会话切换**: 两张截图来自不同会话，一个是空会话（0 条），一个是有数据的会话（19962 条）
3. **前端状态未同步**: 加载完成后标题栏的计数器更新了，但进度条未消失

---

### P7: 右侧统计数字与左侧树不匹配

**现象**: 
- 截图 3 右侧显示"推理 2, 工具流 1, 助手 7, 事件 5"，但 Step 3.1 标题显示"2,420 条"
- 截图 4 右侧显示"工具流 3, 助手 6, 事件 5"，Step 3.2 标题显示"2,420 条"

**可能原因**:

1. **统计口径不一致**: 右侧统计的是"合并后的事件组数量"（推理合并为 2 段、工具流合并为 1 组），左侧标题显示的是"原始事件数"（2420 条未合并的 chunks）
2. **事件计数逻辑错误**: 右侧"事件 5"可能是该 Step 内的独立事件数（非 chunks），但显示位置让人误以为是总事件数
3. **Step 标题数据错误**: "2,420 条"可能是整个 Turn 3 的事件数被错误赋值给了 Step

---

## 根因推测

综合以上问题，最可能的根因是：

### 后端层面

| 问题 | 根因 |
|------|------|
| P1 加载卡 30% | 解析器在处理大文件（19962 条）时性能不足或遇到异常中断 |
| P2 摘要内容缺失 | 摘要聚合 API 未实现，只有事件列表 API |
| P3 无预览 | 事件预览生成函数未实现或数据字段提取错误 |
| P5 文本截断 | 分片合并逻辑未实现，只取了首个分片 |

### 前端层面

| 问题 | 根因 |
|------|------|
| P4 红色箭头 | 节点状态判断逻辑有 bug，正常节点不应标红 |
| P6 数量不一致 | 加载状态管理有竞态条件，进度条和计数器未同步更新 |
| P7 统计不匹配 | 合并后计数和原始计数混用，未做区分标注 |

---

## 修复优先级

| 优先级 | 问题 | 修复建议 |
|--------|------|---------|
| P0 | P3 无预览 | 实现事件预览生成函数，按需求文档 2.4 节每种类型定制预览 |
| P0 | P1 加载卡住 | 增加分页加载或流式解析，避免一次性加载 19962 条 |
| P0 | P2 摘要缺失 | 实现摘要聚合 API，返回工具统计/Token用量/审批统计 |
| P1 | P4 红色箭头 | 检查节点状态逻辑，修正 CSS 样式 |
| P1 | P5 文本截断 | 实现分片合并，预览取前 80 字符 + "..." |
| P2 | P7 统计不匹配 | 明确标注"合并后 N 组 / 原始 M 条" |
| P2 | P6 数量不一致 | 修复加载状态管理，进度完成后再更新计数器 |
